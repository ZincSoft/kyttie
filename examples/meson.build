project('kyttie-examples', 'c')

incdirs = [
  join_paths('..', 'linux_source_tree.ignore', 'include'),
  join_paths('..', 'include'),
]

executable('cp_socket', 'cp_socket.c', include_directories: incdirs)

